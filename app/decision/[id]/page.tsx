import Link from "next/link";import {notFound,redirect} from "next/navigation";import {ArrowLeft,ArrowRight,Calendar,FileText,Users,WalletCards,ShieldCheck,ExternalLink} from "lucide-react";import {isAdmin} from "@/lib/auth";import {getAudit,getDecisionById} from "@/lib/data";import {StatusBadge} from "@/components/StatusBadge";import {HashBlock} from "@/components/HashBlock";import {Logo} from "@/components/Logo";import {LanguageSwitch} from "@/components/LanguageSwitch";import {resolveLang,withLang,locale} from "@/lib/i18n";
export const dynamic="force-dynamic";
export default async function DecisionPage({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{lang?:string}>}){const [{id},q]=await Promise.all([params,searchParams]);const lang=resolveLang(q.lang);const ar=lang==="ar";if(!(await isAdmin()))redirect(withLang("/login",lang));const d=await getDecisionById(id);if(!d)notFound();const audit=await getAudit(d.id);return <main className="app-bg" dir={ar?"rtl":"ltr"}><header className="app-header"><Link href={withLang("/",lang)}><Logo/></Link><nav><Link href={withLang(`/verify/${d.code}`,lang)} className="header-link"><ExternalLink size={15}/>{ar?"صفحة التحقق العامة":"Public verification"}</Link><LanguageSwitch lang={lang}/></nav></header><div className="record-container"><Link href={withLang("/dashboard",lang)} className="back-link">{ar?<ArrowRight size={16}/>:<ArrowLeft size={16}/>} {ar?"العودة للسجلات":"Back to records"}</Link><div className="record-title"><div><div className="record-code">{d.code}</div><h1>{d.title}</h1><p>{d.summary}</p></div><StatusBadge status={d.status} lang={lang}/></div><div className="record-grid"><section className="record-card"><h2>{ar?"تفاصيل القرار":"Decision details"}</h2><div className="detail-grid"><div><Calendar/><span>{ar?"أُنشئ":"Created"}</span><strong>{new Date(d.created_at).toLocaleString(locale(lang))}</strong></div><div><Users/><span>{ar?"الأطراف":"Parties"}</span><strong>{d.parties.length?d.parties.join(ar?"، ":", "):(ar?"غير محدد":"Not specified")}</strong></div><div><WalletCards/><span>{ar?"المبلغ":"Amount"}</span><strong>{d.amount!==null?`${Number(d.amount).toLocaleString(locale(lang))} ${d.currency}`:(ar?"غير محدد":"Not specified")}</strong></div><div><FileText/><span>{ar?"ملف الدليل":"Evidence"}</span><strong>{d.evidence_name}</strong></div></div><HashBlock hash={d.evidence_hash} lang={lang}/></section><section className="record-card"><h2>{ar?"حالة القرار":"Decision state"}</h2>{d.status==="PENDING"?<div className="decision-actions"><form action={`/api/decisions/${d.id}/approve`} method="post"><textarea name="note" placeholder={ar?"ملاحظة الاعتماد (اختياري)":"Approval note (optional)"}/><button className="button button-primary button-wide">{ar?"اعتماد القرار":"Approve decision"}</button></form><form action={`/api/decisions/${d.id}/reject`} method="post"><button className="button button-danger button-wide">{ar?"رفض القرار":"Reject decision"}</button></form></div>:<div className="locked-state"><ShieldCheck size={32}/><strong>{d.status==="APPROVED"?(ar?"تم قفل الدليل على السجل المعتمد":"Evidence locked to approved record"):(ar?"تم رفض القرار":"Decision rejected")}</strong><p>{d.decision_note||(ar?"بدون ملاحظة قرار.":"No decision note.")}</p>{d.decided_at&&<span>{new Date(d.decided_at).toLocaleString(locale(lang))}</span>}</div>}</section></div><section className="record-card audit-card"><h2>{ar?"سجل التدقيق":"Audit trail"}</h2>{audit.map(a=><div className="audit-row" key={a.id}><div className="audit-pin"/><div><strong>{a.event_type==="CREATED"?(ar?"إنشاء السجل":"Record created"):a.event_type==="APPROVED"?(ar?"اعتماد القرار":"Decision approved"):a.event_type==="REJECTED"?(ar?"رفض القرار":"Decision rejected"):a.event_type}</strong><p>{ar?(a.event_type==="CREATED"?"تم إنشاء سجل القرار وحساب بصمة الدليل.":a.event_type==="APPROVED"?"تم اعتماد القرار وقفل بصمة الدليل على السجل.":a.message):a.message}</p></div><time>{new Date(a.created_at).toLocaleString(locale(lang))}</time></div>)}</section></div></main>}
